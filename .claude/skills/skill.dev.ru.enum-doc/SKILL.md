---
name: "skill.dev.ru.enum-doc"
description: >-
  Defines the standard documentation format for enumerations (enums): table structure with
  system name, serialized value, human-readable name, and comment. Covers document-level
  implementation type (int enum vs string record), naming conventions by stack, mandatory
  rules for DB/inter-service serialization and the `undefined` zero-value convention. Use
  when documenting any enum or discriminated union used in entities, DTOs, API contracts,
  or inter-service communication.
---

# Claude Code Adapter

This is a generated adapter for the canonical shared skill:

`../../../.agents/skills/skill.dev.ru.enum-doc/SKILL.md`

When this skill is invoked:

1. Read the canonical `SKILL.md` above.
2. Follow the canonical instructions and only load referenced files when needed.
3. Do not treat this adapter as the source of truth.

Regenerate adapters with:

```bash
node scripts/ai-template-indexing/sync-claude-skill-adapters.mjs
```
