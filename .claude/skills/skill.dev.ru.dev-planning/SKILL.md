---
name: "skill.dev.ru.dev-planning"
description: >-
  Defines a standardized development planning approach with dev-plan and dev-task
  artifacts. Each dev-plan lives inside its dev-request folder at
  docs/dev-requests/dev-request.r-nnn.<name>/dev-planning/dev-plan.r-nnn.<name>/
  containing the plan file and flat dev-task files dev-task.r-nnn.t-nnn.<name>.md. Use
  when starting implementation from architecture artifacts (requirements, C4, ADR, DDS),
  when creating or updating a dev-plan, or when asking about dev-task structure,
  status/version lifecycle, or planning changelog.
---

# Claude Code Adapter

This is a generated adapter for the canonical shared skill:

`../../../.agents/skills/skill.dev.ru.dev-planning/SKILL.md`

When this skill is invoked:

1. Read the canonical `SKILL.md` above.
2. Follow the canonical instructions and only load referenced files when needed.
3. Do not treat this adapter as the source of truth.

Regenerate adapters with:

```bash
node scripts/ai-template-indexing/sync-claude-skill-adapters.mjs
```
