---
name: "skill.dev.ru.data-model-table"
description: >-
  Defines a standard table format for documenting data models: entities, DTOs, JSON
  payloads, inter-service contracts, and database schemas. Covers column semantics,
  nesting strategy selection (N-numbering as default, dot-path for shallow JSON ≤ 3
  levels), array notation, `?`-based nullable/optional notation on types, format
  conventions, and stack-aware naming. Use when describing any structured model in
  documentation — API contracts, architecture docs, integration passports, or data
  dictionaries.
---

# Claude Code Adapter

This is a generated adapter for the canonical shared skill:

`../../../.agents/skills/skill.dev.ru.data-model-table/SKILL.md`

When this skill is invoked:

1. Read the canonical `SKILL.md` above.
2. Follow the canonical instructions and only load referenced files when needed.
3. Do not treat this adapter as the source of truth.

Regenerate adapters with:

```bash
node scripts/ai-template-indexing/sync-claude-skill-adapters.mjs
```
