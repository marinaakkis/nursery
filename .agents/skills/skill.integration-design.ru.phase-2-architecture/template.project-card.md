<!--
ШАБЛОН: карточка проекта межсервисной интеграции (агрегатор).
Скопируй в рабочую папку integration-request. Self-contained.
Карточка — навигационный агрегатор: крупные разделы (связи, окружения, аутентификация) подключаются
через cross-ссылки на соответствующие артефакты, журнал связанных заявок встраивается таблицей.
DLP: реальные имена систем/организаций и секреты — НЕ в шаблоне (плейсхолдеры); заполняются в рабочем экземпляре.
-->

# Карточка проекта интеграции: <[Система A] ↔ [Система B]>

## 1. Описание

<!-- Из фазы «Концепт». Цель интеграции, бизнес-процесс, который она обслуживает. 3–6 предложений. -->

- **Цель:** <!-- зачем интеграция -->
- **Бизнес-процесс:** <!-- какой процесс обслуживает -->
- **Системы-участники:** [Система A] (инициатор, в контуре компании), [Система B] (получатель, вне контура компании)
- **Контуры:** <!-- какие сетевые контуры задействованы: LAN / WAN / облако -->

## 2. Компоненты

| N | Компонент | Тип (Система/Внешняя/Шлюз/Контейнер) | Описание / ссылка | Комментарии |
|---|---|---|---|---|
| 1 | [Система A] | Система | <!-- роль --> | <!-- окружения/URL, IP для IP-фильтрации, ссылки на офиц. документацию и пр. --> |
| 2 | [Шлюз] | Шлюз | <!-- роль и функции шлюза --> | |
| 3 | [Система B] | Внешняя система | | |

> Колонка «Комментарии» — поле для всего, что не укладывается в остальные: окружения и URL, IP-адреса для IP-фильтрации, ссылки на внешнюю официальную документацию, особенности.

## 3. Связи

Детальная тех-таблица связей (эндпоинт, описание, формат, тип взаимодействия, аутентификация, протокол, частота, направление) и таблица «Надёжность» — в [`general-description`](general-description.md). Направление = кто кого вызывает.

## 4. Окружения

Окружения систем и их связи — в [`environments`](environments.md).

## 5. API и данные

- Эндпоинты целевого сервиса — [`api-endpoints`](api-endpoints.md).
- Модель передаваемых данных (с ПДн/КТ) — [`data-model`](data-model.md).
- Маппинг полей (если у шлюза есть трансформация контрактов) — [`field-mapping`](field-mapping.md).

## 6. Аутентификация и доступы

Модель аутентификации/авторизации, таблица доступов (Service Account → сервис → scope/роль) — в [`auth`](auth.md).

## 7. Связанные заявки

Журнал связанных заявок (встраивается в итоговый документ карточки):

| N | Тип заявки | Система (Service Desk) | ID / ссылка | Статус | Дата | Ответственный |
|---|---|---|---|---|---|---|
| 1 | Согласование (Отдел архитектуры) | <!-- проект SD --> | <!-- ARCHI-#### --> | <!-- Создана/На рассмотрении/Согласовано/Отклонено --> | | |
| 2 | Согласование (Отдел информационной безопасности) | | | | | |
| 3 | Создание Service Account / УЗ | | | | | |

> Источник и формат журнала — [`related-requests-log`](related-requests-log.md). В итоговую карточку таблица копируется (а не оставляется ссылкой), чтобы согласующий видел заявки на месте.

## 8. Диаграммы (по запросу)

| Диаграмма | Формат | Ссылка |
|---|---|---|
| C2 (контейнерная) | mermaid | <!-- путь/блок --> |
| Sequence (для сложных процессов) | mermaid | |
| Схема окружений | mermaid | |

> Диаграммы — производные от таблиц, для людей. Источник истины — таблицы в связанных артефактах (разделы 3–6).
