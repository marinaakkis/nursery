<!-- Template path: .agents/skills/skill.integration.ru.api-endpoint-doc/template.api-contract.md -->
<!-- Output path:   docs/dev-requests/dev-request.r-<nnn>.<system-request-name>/design/api-requirements/api.r-<nnn>.<system-request-name>.md -->
<!-- After merge:   docs/api-requirements/api.<project-name>.<domain>.md -->
<!-- Формат эндпоинта: skill.integration.ru.api-endpoint-doc -->
<!-- Формат тела запроса/ответа и моделей: skill.dev.ru.data-model-table -->

# API Контракт: r-<nnn> <system-request-name>

## Мета

| Поле | Значение |
|---|---|
| Запрос на разработку | `r-<nnn>` |
| Сервис | <!-- название сервиса --> |
| Домен | <!-- domain --> |
| Версия API | <!-- v1 / v2 --> |
| Дата | <!-- YYYY-MM-DD --> |
| Статус | Draft / Approved |

---

## Обзор изменений

<!-- Кратко: что добавляется / изменяется / удаляется в API -->

---

## Эндпоинты

<!-- Каждый эндпоинт оформляется по skill.integration.ru.api-endpoint-doc. Добавляй секции по мере необходимости. -->

### <Название операции, например: Создание пользователя>

<!-- Краткое описание: что делает, зачем нужна. 1–2 предложения. -->

#### Запрос

1. **HTTP-метод:** `<POST | GET | PUT | PATCH | DELETE>`
2. **Путь:** `<например, /api/v1/...>`
3. **Авторизация:** `<Bearer JWT / API Key / None>`
4. **Параметры:**

| N | Параметр | Тип параметра | Название | Тип данных | Формат | Комментарий |
|---|---|---|---|---|---|---|
| 1 | `id` | `path` | Идентификатор | `string` | GUID | |
| 2 | `filter?` | `query` | Фильтр | `string?` | — | Необязателен |

<!-- Тип параметра: только path / query / header. Необязательный параметр: суффикс ? на имени (filter?). -->
<!-- Стандартные заголовки (Authorization, Content-Type) в таблицу не включаются. -->

*Если тело запроса отсутствует — пп. 5 и 6 пропускаются.*

5. **Content-Type:** `application/json`
6. **Тело запроса:**

<!-- Таблица по skill.dev.ru.data-model-table. Необязательные/nullable поля — через ? на типе (string?). Колонки Обязательное/Nullable не добавляются. -->

| N | Свойство модели | Наименование | Тип данных | Формат | Комментарий |
|---|---|---|---|---|---|
| | | | | | |

---

#### Ответ

1. **Content-Type:** `application/json`
2. **Тело ответа:**

<!-- Таблица по skill.dev.ru.data-model-table. -->

| N | Свойство модели | Наименование | Тип данных | Формат | Комментарий |
|---|---|---|---|---|---|
| | | | | | |

3. **Статусы:**

<!-- Для каждого статуса обязательно заполнять Условие и Тело ответа. -->

| N | Статус | Условие | Тело ответа | Комментарий |
|---|---|---|---|---|
| 1 | `200` | Успешное выполнение | `ResponseDto` | |
| 2 | `400` | Ошибка валидации входных данных | `ValidationErrorDto` | |
| 3 | `401` | Не авторизован | — | |
| 4 | `403` | Доступ запрещён | — | |
| 5 | `404` | Ресурс не найден | `ErrorDto` | |
| 6 | `500` | Внутренняя ошибка сервера | `ErrorDto` | |

---

## Модели данных

<!-- Таблица по skill.dev.ru.data-model-table. Стратегия A (N-нумерация) — по умолчанию; Стратегия Б (dot-path) — только для JSON с вложенностью ≤ 3 уровней. -->

### `ModelName`

| N | Свойство модели | Наименование | Тип данных | Формат | Комментарий |
|---|---|---|---|---|---|
| | | | | | |

---

## Изменения по сравнению с текущей версией

<!-- Список breaking changes, backwards-compatible changes -->

| Тип | Эндпоинт / Поле | Описание |
|---|---|---|
| Breaking | | |
| Compatible | | |

---

## Чеклист перед апрувом

- [ ] Эндпоинты оформлены по `skill.integration.ru.api-endpoint-doc`: метод, путь, авторизация, параметры, тело, статусы
- [ ] Параметры: тип только `path`, `query`, `header`; необязательные помечены `?` на имени; стандартные заголовки не дублируются
- [ ] Тела запроса/ответа и модели описаны таблицей по `skill.dev.ru.data-model-table`; колонок `Обязательное`/`Nullable` нет — обязательность через `?` на типе
- [ ] Для каждого статуса заполнено «Условие» и «Тело ответа»
- [ ] Breaking changes выявлены и задокументированы
- [ ] После завершения всех фаз: смержить в `docs/api-requirements/api.<project-name>.<domain>.md`
