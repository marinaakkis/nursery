---
name: skill.ai-sdlc.ru.phase-1-research.reference
description: >
  Full template for the Phase 1 research document. Referenced by SKILL.md.
  Use this template to fill in AS-IS facts when executing phase-1-research.
---

# Шаблон: Research-документ

```markdown
# Research: R-<NNN>.<system-request-name>

## Задача
<!-- Кратко: что нужно сделать. 1–3 предложения из тикета или user story. -->
<краткое описание — что нужно сделать>

## Затронутые компоненты
<!-- Перечисли все компоненты, которых касается задача.
     Ссылка на файл — относительный путь от корня репозитория.
     Строки — диапазон строк, например `45–120` или `N/A`.
     Если файла ещё нет (новая фича) — укажи `N/A` в полях "Ссылка на файл" и "Строки". -->

| # | Компонент | Ссылка на файл | Строки | Описание |
|---|---|---|---|---|
<!-- Пример (удали или замени на реальные данные): -->
| 1 | OrderService | `src/Orders/Application/OrderService.cs` | 45–120 | Основной сервис обработки заказов |

## Архитектурный контекст
<!-- Опиши, как устроен затронутый код прямо сейчас.
     Структура ниже — возможный пример, не обязательная схема.
     Заполни по фактическому состоянию кода.
     Если кодовая база отсутствует (новый проект) — этот раздел пропускается. -->

**Слои** _(пример — возможная структура, не исчерпывающая)_:
`API (Controllers)` → `Application (Services, Commands/Queries)` → `Domain (Entities, Domain Events)` → `Infrastructure (Repositories, EF DbContext)`

**Паттерны** _(пример — возможный список, не исчерпывающий)_:
- CQRS через MediatR (команды в `Application/Commands/`, запросы в `Application/Queries/`)
- Repository pattern: интерфейсы в `Domain/`, реализации в `Infrastructure/`

**Зависимости** _(пример — возможная цепочка, не исчерпывающая)_:
`OrderController` → `IMediator` → `CreateOrderCommandHandler` → `IOrderRepository` → `OrderDbContext`

**Артефакты** _(пример — возможный список, не исчерпывающий)_:
- БД: таблицы `orders`, `order_items` (PostgreSQL, миграции в `Infrastructure/Migrations/`)
- API-контракт: `POST /api/v1/orders` (OpenAPI spec в `docs/api/`)

## Доменные модели
<!-- Опиши каждую сущность, которой касается задача, отдельным подразделом.
     Дублируй блок ### Сущность: ... для каждой следующей сущности. -->

### Сущность: <Название сущности>

**Системное название:** `<ClassName / TableName>`
**Описание:** <что это за сущность, её роль в домене>
**Ссылка на файл:** `<путь/к/файлу/Сущность.cs>` _(если файла ещё нет — `— (планируется: <предполагаемый путь>)`)_

#### Атрибуты

<!-- Перечисли ключевые поля: идентификаторы, статусы, внешние ключи, важные значения. -->

| # | Поле | Тип | Описание |
|---|---|---|---|
<!-- Пример (удали или замени на реальные данные): -->
| 1 | Id | `Guid` | Уникальный идентификатор |
| 2 | Status | `OrderStatus` | Статус заказа (`Draft`, `Confirmed`, `Cancelled`) |

#### Связи

<!-- Тип связи: 1:1 / 1:N / N:M
     Атрибуты связи — поля (FK / join-ключи), через которые формируется связь. -->

| # | Связанная сущность | Тип связи | Атрибуты связи | Описание |
|---|---|---|---|---|
<!-- Пример (удали или замени на реальные данные): -->
| 1 | OrderItem | `1:N` | `OrderItem.OrderId → Order.Id` | Заказ содержит позиции |

---

## Внешние зависимости / интеграции
<!-- Перечисли внешние сервисы, API, БД, очереди, затронутые задачей.
     Направление — `Входящее` (сервис получает данные извне) /
                   `Исходящее` (сервис отправляет данные наружу) /
                   `Двустороннее`.
     Ссылка на файл — где в коде находится точка взаимодействия. -->

| # | Сервис / API / БД | Протокол | Метод / Эндпоинт | Направление | Ссылка на файл | Описание |
|---|---|---|---|---|---|---|
<!-- Пример (удали или замени на реальные данные): -->
| 1 | PaymentService | REST/HTTPS | `POST /payments` | Исходящее | `src/Infrastructure/Clients/PaymentClient.cs` | Создание платежа при оформлении заказа |

## Граничные условия
<!-- Задокументируй транзакционность, конкуренцию, ограничения и особые случаи.
     Типы не исчерпывающие — добавляй новые при необходимости.
     Ссылка на файл — где в коде это реализовано или должно быть учтено. -->

| # | Тип | Описание | Ссылка на файл | Строки |
|---|---|---|---|---|
<!-- Пример (удали или замени на реальные данные): -->
| 1 | Транзакционность | Создание заказа и резервирование остатков выполняются в одной транзакции | `src/Application/Commands/CreateOrderHandler.cs` | 55–80 |

Возможные типы (список не исчерпывающий): `Транзакционность` / `Конкурентность` / `Ограничение` / `Особый случай` / `Идемпотентность` / `Лимит / квота`

## Что не затронуто
<!-- Явно перечисли компоненты и файлы, которые менять не нужно.
     Ссылка на файл — относительный путь от корня репозитория. -->

| # | Компонент | Ссылка на файл | Причина |
|---|---|---|---|
<!-- Пример (удали или замени на реальные данные): -->
| 1 | NotificationService | `src/Notifications/NotificationService.cs` | Уведомления не входят в scope задачи |
```
