<!-- Template: docs-actualization-plan -->
<!-- Output: docs/dev-requests/dev-request.r-<nnn>.<system-request-name>/docs-actualization-plan.r-<nnn>.<system-request-name>.md -->
<!-- Создаётся командой command.ai-sdlc.ru.actualize-main-docs (правило rule.ai-sdlc.ru.docs-actualization-after-dev-request). -->

# Docs Actualization Plan: r-<nnn> <system-request-name>

## Шапка

| Поле | Значение |
|---|---|
| Dev-request | `r-<nnn>` <system-request-name> |
| Дата | <!-- YYYY-MM-DD --> |
| Профиль `docs` источника | <!-- compact / full --> |
| Якорь | `docs/architecture.md` |
| Сводный риск | <!-- низкий / средний / высокий --> |

**Легенда «Решение»:** `✅ Принять` / `❌ Отклонить` / `⏳ Запланировать` / `✎ Редактировать`.
**Дефолты (агент проставляет, пользователь правит):** глобально значимые → `✅ Принять`; локальные кандидаты → `❌ Отклонить`. Локальный артефакт, уже присутствующий в `docs/`, помещается в Таблицу 1 с `✅ Принять`.
**Действие:** `Create` (нет в `docs/` → создать) / `Update` (есть → дельта) / `Deprecate` (неактуален целиком → пометить/удалить).

## Таблица 1 — Глобально значимые артефакты (дефолт `✅ Принять`)

| # | Артефакт | Источник (путь от корня репо) | Целевой артефакт | Действие | Решение | Статус |
|---|---|---|---|---|---|---|
<!-- пример: -->
| 1 | C4 Container (C2), системный | `docs/dev-requests/dev-request.r-<nnn>.<name>/design/c4-diagrams/c2.r-<nnn>.*.md` | `docs/c4-diagrams/c2.system.md` | Update | ✅ Принять | Запланировано |

## Таблица 2 — Локальные кандидаты (дефолт `❌ Отклонить`)

| # | Локальный артефакт | Источник (путь от корня репо) | Возможный целевой артефакт | Уже в `docs/`? | Решение | Статус |
|---|---|---|---|---|---|---|
<!-- пример: -->
| 1 | Sequence сценария X | `docs/dev-requests/dev-request.r-<nnn>.<name>/design/process-diagrams/sd.*.md` | `docs/process-diagrams/sd.x.md` | нет | ❌ Отклонить | — |

> Если «Уже в `docs/`?» = `да` — строку перенести в Таблицу 1 (дефолт `✅ Принять`).

## Блок «Переход»

Заполняется для каждой строки с `Решение = ✅ Принять` (кроме `Deprecate` без замены).

### <#> <Целевой артефакт>
- **Сейчас (AS-IS):** <!-- текущее состояние целевого артефакта: `отсутствует` / кратко -->
- **Станет (TO-BE):** <!-- целевое состояние -->
- **Дельта:** <!-- конкретные правки: узлы/связи C4, строки ERD, поля контракта, новый ADR — отдельно от TO-BE -->
- **Почему:** <!-- какое изменение кода / межсервисной связи / решение это отражает -->
- **Формат/профиль:** <!-- диаграмма/таблица в docs/; развернуть ли compact-таблицу источника в диаграмму -->
- **Зона обновления:** <!-- целиком / между AUTOGEN-маркерами / ручной merge-review + риск перезаписи -->
