<!-- Template path: .agents/skills/skill.integration.ru.integration-doc/template.api-external-contract.md -->
<!-- Output path:   docs/dev-requests/dev-request.r-<nnn>.<system-request-name>/design/api-requirements/api.external.r-<nnn>.<system-request-name>.md -->
<!-- After merge:   docs/api-requirements/api.external.<project-name>.<domain>.md -->
<!-- Обзор интеграции (аутентификация, надёжность, реестр вызовов): skill.integration.ru.integration-doc -->
<!-- Формат эндпоинта: skill.integration.ru.api-endpoint-doc -->
<!-- Формат тела запроса/ответа и моделей: skill.dev.ru.data-model-table -->

# API Контракт (внешняя интеграция): r-<nnn> <system-request-name>

## Мета

| Поле | Значение |
|---|---|
| Запрос на разработку | `r-<nnn>` |
| Внешняя система | <!-- название внешней системы --> |
| Base URL | <!-- https://api.example.com/v1 --> |
| Тип интеграции | <!-- REST / gRPC / SOAP / Webhook --> |
| Дата | <!-- YYYY-MM-DD --> |
| Статус | Draft / Approved |
| Паспорт интеграции | `docs/integrations/integration-passport.<name>.md` |

---

## Обзор интеграции

<!-- Кратко: зачем нужна интеграция, какой сценарий она покрывает -->

---

## Аутентификация и авторизация

| Параметр | Значение |
|---|---|
| **Метод аутентификации** | `<Basic Auth / JWT / OAuth2 / API Key / mTLS / Без аутентификации>` |
| **Где хранятся credentials** | `<Vault / Env variables / K8s Secrets>` |
| **Роли / доступы** | `<Необходимые роли или разрешения на стороне обработчика; или «Не требуется»>` |

---

## Надёжность

| Параметр | Значение |
|---|---|
| **Таймаут запроса** | `<мс, например: 3000 мс>` |
| **Retry (количество)** | `<3>` |
| **Retry стратегия** | `<Exponential backoff>` |
| **Circuit breaker** | `<Да / Нет; если да — порог срабатывания>` |
| **Fallback** | `<описание поведения при недоступности>` |

---

## Реестр вызовов

<!-- Сводная таблица по skill.integration.ru.integration-doc. -->
<!-- Тип вызова: sync-pull / sync-push / sync-polling (указывать частоту: sync-polling 1/min) / async. -->

| N | HTTP-метод | Путь | Описание | Content-Type запроса | Content-Type ответа | Тип вызова | Комментарий |
|---|---|---|---|---|---|---|---|
| 1 | `POST` | `/path` | Описание | `application/json` | `application/json` | `sync-push` | |

---

## Эндпоинты (детально)

<!-- Каждый эндпоинт оформляется по skill.integration.ru.api-endpoint-doc. Добавляй секции по мере необходимости. -->

### <Название операции>

<!-- Краткое описание: что делает, зачем нужна. 1–2 предложения. -->

#### Запрос

1. **HTTP-метод:** `<POST | GET | PUT | PATCH | DELETE>`
2. **Путь:** `</path>`
3. **Параметры:**

| N | Параметр | Тип параметра | Название | Тип данных | Формат | Комментарий |
|---|---|---|---|---|---|---|
| | | | | | | |

<!-- Тип параметра: только path / query / header. Необязательный параметр: суффикс ? на имени. -->

*Если тело запроса отсутствует — пп. 4 и 5 пропускаются.*

4. **Content-Type:** `application/json`
5. **Тело запроса:**

<!-- Таблица по skill.dev.ru.data-model-table. Необязательные/nullable поля — через ? на типе (string?). -->

| N | Свойство модели | Наименование | Тип данных | Формат | Комментарий |
|---|---|---|---|---|---|
| | | | | | |

---

#### Ответ

1. **Content-Type:** `application/json`
2. **Тело ответа:**

<!-- Таблица по skill.dev.ru.data-model-table. -->

| N | Свойство модели | Наименование | Тип данных | Формат | Комментарий |
|---|---|---|---|---|---|
| | | | | | |

3. **Статусы и обработка ошибок:**

<!-- Для каждого статуса обязательно заполнять Условие и Наша реакция. -->

| N | Статус | Условие | Тело ответа | Наша реакция | Комментарий |
|---|---|---|---|---|---|
| 1 | `200` | Успех | `ResponseDto` | Обработать ответ | |
| 2 | `400` | Некорректный запрос | `ErrorDto` | Логировать, не повторять | |
| 3 | `401` | Не авторизован | — | Обновить токен / алертировать | |
| 4 | `429` | Rate limit | — | Exponential backoff | |
| 5 | `500` | Ошибка внешней системы | — | Retry + circuit breaker | |

---

## Безопасность

| Аспект | Описание |
|---|---|
| Хранение credentials | <!-- Vault / Env var / Secret Manager --> |
| Передача данных | <!-- TLS 1.2+ --> |
| PII в запросах | <!-- Да / Нет; если да — как маскируется --> |
| Логирование | <!-- Что логируется, что исключается из логов --> |

---

## Модели данных

<!-- Таблица по skill.dev.ru.data-model-table. Стратегия A (N-нумерация) — по умолчанию; Стратегия Б (dot-path) — только для JSON с вложенностью ≤ 3 уровней. -->

### `ModelName`

| N | Свойство модели | Наименование | Тип данных | Формат | Комментарий |
|---|---|---|---|---|---|
| | | | | | |

---

## Чеклист перед апрувом

- [ ] Паспорт интеграции создан / обновлён
- [ ] Аутентификация и авторизация заполнены
- [ ] Надёжность: Timeout, Retry, Circuit breaker, Fallback определены
- [ ] Реестр вызовов заполнен (по `skill.integration.ru.integration-doc`): тип вызова из справочника; для `sync-polling` указана частота
- [ ] Детальные эндпоинты описаны по `skill.integration.ru.api-endpoint-doc`
- [ ] Параметры: тип только `path`, `query`, `header`; необязательные помечены `?` на имени; стандартные заголовки не дублируются
- [ ] Тела запроса/ответа и модели описаны таблицей по `skill.dev.ru.data-model-table`; колонок `Обязательное`/`Nullable` нет
- [ ] Для каждого статуса заполнено «Условие» и «Наша реакция»
- [ ] Credentials хранятся безопасно (не в коде)
- [ ] PII-поля идентифицированы и маскируются в логах
- [ ] После завершения всех фаз: смержить в `docs/api-requirements/api.external.<project-name>.<domain>.md`
