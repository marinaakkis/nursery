#!/usr/bin/env bash
# Единая проверка проекта. Пока заглушка: стек ещё не инициализирован.
# Наполняется на шаге «скелет приложения» — шаги ниже уже фиксируют порядок.

set -euo pipefail

echo "check.sh — проверка проекта"
echo
echo "  1. typecheck   — строгая типизация, без ошибок"
echo "  2. lint        — линтер и форматтер проходят чисто"
echo "  3. тесты       — unit и integration, включая тест на гонку за последний экземпляр"
echo "  4. миграции    — применяются на чистой БД с нуля"
echo "  5. сборка      — production-сборка проходит"
echo
echo "не реализовано: стек ещё не инициализирован" >&2
exit 1
